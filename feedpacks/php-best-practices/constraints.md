# Constraints

- Never suggest `mysql_*` functions
- Never use `goto`
- Avoid global state and static methods
- No `var_dump` / `print_r` in production code
- Avoid deep nesting (max 3 levels)
- Classes max 200 lines
- Methods max 30 lines
